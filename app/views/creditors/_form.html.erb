<%= form_with(model: creditor, local: true) do |form| %>
  <% if creditor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(creditor.errors.count, "error") %> prohibited this creditor from being saved:</h2>

      <ul>
        <% creditor.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user %>
    <%= form.text_field :user %>
  </div>

  <div class="field">
    <%= form.label :transaction_id %>
    <%= form.text_field :transaction_id %>
  </div>

  <div class="field">
    <%= form.label :confirmed %>
    <%= form.check_box :confirmed %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
